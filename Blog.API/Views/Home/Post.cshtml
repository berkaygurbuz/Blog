@model Post
@using Blog.Entities
@{ 

}

@if (!String.IsNullOrEmpty(Model.Image))
{
    var imagePath = $"{Model.Image}".Remove(0, 7);
    <img src="@imagePath" width="300" />
}


<h2>@Model.Title</h2>

<p>@Model.Body</p>